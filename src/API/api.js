const BASE_URL = 'http://localhost:3000';
const API = {
    REGISTER: `${BASE_URL}/register`,
    LOGIN: `${BASE_URL}/login`,
    APPOINTMENTS: `${BASE_URL}/appointments`,
    BOOK_APPOINTMENT: `${BASE_URL}/book`,
    PROFILE: `${BASE_URL}/profile`,
    USERs:`${BASE_URL}/users`,
};

export default API;